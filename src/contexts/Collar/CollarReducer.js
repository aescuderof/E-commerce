const GuitarReducer = (globalState, action) => {
    switch (action.type) {
        case "ACTUALIZAR":
            break;

        case "LOGOUT":
            break;

        default:
            break;
    }

    return globalState;
};

export default CollarReducer;